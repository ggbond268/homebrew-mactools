# Homebrew Tap for MacTools

Install [MacTools](https://github.com/ggbond268/MacTools) with Homebrew:

```bash
brew tap ggbond268/mactools
brew install --cask mactools
```

Update:

```bash
brew update
brew upgrade --cask mactools
```
